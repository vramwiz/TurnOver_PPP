# TurnOver_PPP 作業ノート

## 目的

このファイルは、TurnOver_PPPの現在の実装、ビルド・配備規則、実装・保守ルールをまとめる。完了済みの段階別作業ログは残さず、作業再開に必要な現状だけを記録する。

## 現在の状態

- AviUtl2用フィルター`TurnOver_PPP`として、指定した布範囲を2点のつまみで変形する。
- 画像端まで届く範囲は全面画像用の従来演算、それ以外は選択範囲だけを動かす部分選択演算へ自動で切り替える。
- 全面画像は`縮小率`（10～100%、初期値100%）で変形前に中央基準の透明余白を作れる。中間画像は生成せず、変形と縮小を合成した逆参照座標から元画像を1回だけ補間取得する。部分選択演算には適用しない。
- スカート向け部分選択では、外周の上側経路を固定上面として自動判定する。設定画面ではこの経路をオレンジ色で表示する。
- 固定上面と左右上端は動かさず、下側ほど変形へ滑らかに追従させる。実行時は事前計算済みの固定率を参照する軽量方式とする。
- 固定率は固定上面からの一定半径ではなく、固定上面と裾側境界までの距離比を布全体の深さとして正規化する。大きくめくった場合も狭い遷移帯で座標が反転しないようにする。
- 裾側境界は輪郭線分の位置しきい値ではなく、`固定辺`と正反対の辺を曲線モデルから明示的に抽出する。左右固定時に上辺・底辺が途中から裾側境界へ誤分類され、重力のY変位が中央以降で一定になる問題を防ぐ。
- 手動つまみを使う「めくる」では、輪郭の縁取りを残さないため解像度連動2～12pxの安全余白を使用する。
- 手動つまみが0で、風または波打ちだけを使う「揺らす」では、隣接する手・袖・別の布を巻き込まない2pxの狭い安全余白へ切り替える。
- 布範囲データは`TPP2`形式で`なびき方`（従来／しなり／揺れ／なびく）と`固定辺`（上／下／左／右）を保存できる。旧`TPP1`・`SPP1`・`SPP2`・`SPP3`は「従来・上」として読み込む。設定画面で両方を選択でき、選択中の固定辺に対応する輪郭経路をオレンジ色で表示する。`固定辺`は設定プレビューと本番の部分選択変形の固定率マップへ接続済み。
- 設定画面の追加項目はDFM内でUnicodeコード値として保持し、環境依存の文字化けを避ける。`なびき方`と`固定辺`のコンボボックスはダーク背景で所有者描画し、フォント高、文字幅、余白、上部パネルの高さを現在のDPIから実行時に算出する。
- `なびき方=しなり`では、自動風オフセットを手動つまみ移動から分離し、固定率の2乗を使って固定辺0から自由端1まで漸増する曲げとして同時適用する。`なびき方=揺れ`では固定率`a`に対する`a*(2-a)`の追従率を使い、中間部から早く風へ追従させる。`なびき方=なびく`では、固定辺から自由端へ進む共通波に固定疑似乱数位相を合成し、乱れを固定率の2乗で増幅する。疑似乱数は動きの振幅に応じた24px以上のアンカー間隔で生成し、SmoothStep補間する。固定辺からの深さを256段階に分け、左右固定では縦、上下固定では横の隣接ベクトルについて法線成分を最大0.35pxに制限し、接線成分を`sqrt(1-d^2)`として長さ1pxの回転ベクトルを積算する。積算した単調座標から逆関数LUTを作り、法線変位も逆変換後の接線位置で補間するため、右下角で近似誤差により参照座標が範囲外へ出ない。結果を2次元LUTへ保持し、画素処理では深さ2点を線形補間するだけで反復型物理計算は行わない。振幅は`風*0.35 + 波打ち*0.20`、時間は`周期`、空間周波数は`波数`を使う。風は主方向への持続的な押しと上向き揚力として重力と合成する。疑似乱数はフレーム間で固定し、時間位相だけを進めるためちらつかない。全面画像の外周は範囲外1pxの透明色まで双一次補間し、縮小・回転後の底辺や自由端が硬く見切れないようにする。すべて全面画像と部分選択で使用でき、手動のめくりと同時適用できる。
- AviUtl2側の`重力`（0～500px、初期値0px）は、画面下方へ固定率の2乗を掛けた垂れを加える。固定辺は不変、自由端は指定ピクセル分下がる。なびき方、風、波打ち、手動めくりと独立して同時適用できる。
- AviUtl2側の`速度`（0～400%、初期値100%）は、自動アニメーション共通の時間位相へ倍率として適用する。`風`、`揺れ`、`なびく`、`波打ち`が対象で、0%では形状を保ったまま時間進行を停止する。`周期`は基準となる1周のフレーム数として併用し、重力と手動めくりには影響しない。既存項目のインデックスを維持するため、フィルター項目列の末尾へ追加した。
- 上面固定の自然さ、処理負荷、揺らした際にスカート外側の画像が破綻しないことをAviUtl2上で確認済み。

## 共通ライブラリ

TurnOver_PPPは次のSyncroh2共通ユニットを直接参照する。`TurnOver_PPP`と`Syncroh2`は同じ親フォルダーに配置する。

- `..\Syncroh2\AviUtl\Filter\AviUtl2FilterTypes.pas`
  - AviUtl2フィルターSDK型定義
- `..\Syncroh2\AviUtl\Filter\AviUtl2FilterInfoUtils.pas`
  - フレーム、時刻、FPS、画像オブジェクト座標の取得
- `..\Syncroh2\Plugin_Filter\PluginFilterTable.pas`
  - 設定項目と`TFILTER_PLUGIN_TABLE`の登録

共通化できるSDK定義、登録処理、オブジェクト情報取得をプラグイン側に重複実装しない。共通ユニットのABI型定義を変更する場合は、C/C++側SDKとフィールド順、型、アラインメントを照合する。

## プロジェクト構成

- `TurnOver_PPP.dpr`
  - DLLエクスポート境界と使用ユニットを定義する。
- `TurnOver_PPP.dproj`
  - Win64 Debug／Release、出力先、配備処理を定義する。
- `Source\Plugin\Filter`
  - フィルター登録、コールバック、専用GUIの入口を置く。
- `Source\Common`
  - プラグイン内で責務別に分離したモデル、設定、描画、診断を置く。
- `Tests`
  - AviUtl2を起動せずに検証できる単体テストとスモークテストを置く。

## 共通ビルドルール

- Delphi 37.0を使用する。
- 対象プラットフォームはWin64のみとする。
- DebugとReleaseの両構成を保つ。
- 原則としてコンパイル警告0、エラー0で完了とする。
- ビルド前に`C:\ProgramData\aviutl2\Plugin\TurnOver_PPP`がなければ作成する。
- DebugはDLLを`TurnOver_PPP.auf2`へコピーし、調査用のDLLとRSMを残す。
- ReleaseはDLLを`TurnOver_PPP.auf2`へコピーした後、同じ出力先のDLLとRSMを削除する。
- 配置済み`.auf2`はAviUtl2起動中にロックされるため、プラグイン更新時はAviUtl2を終了してからビルドする。

Debug Win64:

```powershell
cmd /c "call ""C:\Program Files (x86)\Embarcadero\Studio\37.0\bin\rsvars.bat"" && msbuild ""D:\DelphiProg\TurnOver_PPP\TurnOver_PPP.dproj"" /t:Build /p:Config=Debug /p:Platform=Win64"
```

Release Win64:

```powershell
cmd /c "call ""C:\Program Files (x86)\Embarcadero\Studio\37.0\bin\rsvars.bat"" && msbuild ""D:\DelphiProg\TurnOver_PPP\TurnOver_PPP.dproj"" /t:Build /p:Config=Release /p:Platform=Win64"
```

配備先:

```text
C:\ProgramData\aviutl2\Plugin\TurnOver_PPP\TurnOver_PPP.auf2
```

## 実装・保守ルール

- フィルターコールバック境界からDelphi例外を外へ漏らさない。
- 毎フレーム処理でファイル再読込、不要なメモリ確保、GUI値の書き戻しを行わない。
- 状態をオブジェクト別に保持する場合は、並列コールバックと破棄時の排他制御を考慮する。
- コメントは処理の言い換えではなく、目的、責務、注意点、状態や値の意味を補う。
- 責務が増えたら専用ユニットへ分け、グローバルな可変状態を避ける。
- Debugのみ`C:\ProgramData\aviutl2\Plugin\TurnOver_PPP\TurnOver_PPP_debug.log`へ診断ログを出力してよい。Releaseでは出力しない。フレームごとの診断BMP保存は通常無効とし、調査時に環境変数`TURNOVER_PPP_DEBUG_DUMP=1`を設定した場合だけ有効にする。

## Git管理ルール

- `.pas`、`.dfm`、`.dpr`、`.dproj`、`.res`、文書、配布・検証に必要なスクリプトと素材を管理する。
- `Win32`、`Win64`、`.dcu`、`.rsm`、`.dll`、`.auf2`、IDEローカル設定、履歴・復旧データは管理しない。
- Pascal、プロジェクト、文書の改行は`.gitattributes`に従う。
- `.res`などのバイナリーファイルはbinaryとして扱う。

## 検証状況

- `StaticDeformerBenchmark`に、固定上面、部分選択、揺れ用の狭いマスク、全面画像の回帰検査を含めている。
- 全面画像の縮小率80%で周囲が透明になり、中央画素が維持される回帰検査を含めている。
- 長方形輪郭で上下左右それぞれの固定辺経路が正しく検出される回帰検査を含めている。
- 上下左右それぞれの固定率マップで、選択辺が0、反対辺が1になる回帰検査を含めている。
- 全面画像の`しなり`で、固定辺の画素が不変のまま、自由端が指定した風オフセット分移動する回帰検査を含めている。
- 全面画像の`揺れ`で、固定辺と自由端の条件を維持しつつ、中間部が`しなり`より早く風へ追従する回帰検査を含めている。
- 全面画像の`なびく`で、固定辺の画素が不変のまま、自由端に風量連動の波変位が生じる回帰検査を含めている。
- 左辺固定の`なびく`で、固定辺を維持しつつ、中央部の隣接する縦1pxベクトルを回転させながら異なる横変位が生じる回帰検査を含めている。
- 左辺固定の強い`なびく`で、自由端における隣接行の輪郭差が5px以内になる回帰検査を含めている。
- 左辺固定の全面画像で`重力=12px`を適用し、左辺が不変のまま右辺が12px下がる回帰検査を含めている。
- 深いスカート輪郭を左右下端から約300px持ち上げる回帰検査を含め、固定上面との補間後も局所座標の反転がないことを確認している。
- 1024×1024のDebugベンチマークは、部分選択約37.5ms/フレーム、全面画像＋波打ち約32.3ms/フレームだった。実行時負荷によって計測値は変動する。
- 全面画像の範囲外1pxを透明色と補間し、縮小境界に半透明画素が生成される回帰検査を含めている。
- 問題が出た縮小67%、重力110、風62、波打ち5、左固定と同じ比率で、右半分の底辺が32列以上連続し、隣接列の段差が3px以内になる回帰検査を含めている。1024pxのカーテン素材でも右下角まで直接レンダリングして目視確認した。
- 左固定の長方形底辺で、固定率が1/4、1/2、3/4位置の順に増加し、3/4位置でも0.95未満である回帰検査を含め、Y変位が右端より前で飽和しないことを確認している。
- 全面画像の出力チェックサムは`2CB825B477384907`。
- Debug Win64はコンパイル警告0・エラー0で成功している。
