# TurnOver_PPP 作業ノート

## 目的

このファイルは、TurnOver_PPPの共通基盤、ビルド・配備規則、実装・保守ルールをまとめる。プラグイン固有の動作仕様は、仕様確定後に別途追加する。

## 現在の状態

- `D:\DelphiProg\Shake_PPP`を初期雛形として複製した。
- プロジェクト名とメインファイル名は`TurnOver_PPP`とする。
- Shake_PPP由来のユニット名と動作固有コードは、不要機能を整理する後工程まで残す。
- 現在の作業ではビルドとAviUtl2の起動・操作を行わない。

## 共通ライブラリ

TurnOver_PPPは次のSyncroh2共通ユニットを直接参照する。`TurnOver_PPP`と`Syncroh2`は同じ親フォルダーに配置する。

- `..\Syncroh2\AviUtl\Filter\AviUtl2FilterTypes.pas`
  - AviUtl2フィルターSDK型定義
- `..\Syncroh2\AviUtl\Filter\AviUtl2FilterInfoUtils.pas`
  - フレーム、時刻、FPS、画像オブジェクト座標の取得
- `..\Syncroh2\Plugin_Filter\PluginFilterTable.pas`
  - 設定項目と`TFILTER_PLUGIN_TABLE`の登録

共通化できるSDK定義、登録処理、オブジェクト情報取得をプラグイン側に重複実装しない。共通ユニットのABI型定義を変更する場合は、C/C++側SDKとフィールド順、型、アラインメントを照合する。

## プロジェクト構成

- `TurnOver_PPP.dpr`
  - DLLエクスポート境界と使用ユニットを定義する。
- `TurnOver_PPP.dproj`
  - Win64 Debug／Release、出力先、配備処理を定義する。
- `Source\Plugin\Filter`
  - フィルター登録、コールバック、専用GUIの入口を置く。
- `Source\Common`
  - プラグイン内で責務別に分離したモデル、設定、描画、診断を置く。
- `Tests`
  - AviUtl2を起動せずに検証できる単体テストとスモークテストを置く。

## 共通ビルドルール

- Delphi 37.0を使用する。
- 対象プラットフォームはWin64のみとする。
- DebugとReleaseの両構成を保つ。
- 原則としてコンパイル警告0、エラー0で完了とする。
- ビルド前に`C:\ProgramData\aviutl2\Plugin\TurnOver_PPP`がなければ作成する。
- DebugはDLLを`TurnOver_PPP.auf2`へコピーし、調査用のDLLとRSMを残す。
- ReleaseはDLLを`TurnOver_PPP.auf2`へコピーした後、同じ出力先のDLLとRSMを削除する。

Debug Win64:

```powershell
cmd /c "call ""C:\Program Files (x86)\Embarcadero\Studio\37.0\bin\rsvars.bat"" && msbuild ""D:\DelphiProg\TurnOver_PPP\TurnOver_PPP.dproj"" /t:Build /p:Config=Debug /p:Platform=Win64"
```

Release Win64:

```powershell
cmd /c "call ""C:\Program Files (x86)\Embarcadero\Studio\37.0\bin\rsvars.bat"" && msbuild ""D:\DelphiProg\TurnOver_PPP\TurnOver_PPP.dproj"" /t:Build /p:Config=Release /p:Platform=Win64"
```

配備先:

```text
C:\ProgramData\aviutl2\Plugin\TurnOver_PPP\TurnOver_PPP.auf2
```

## 実装・保守ルール

- フィルターコールバック境界からDelphi例外を外へ漏らさない。
- 毎フレーム処理でファイル再読込、不要なメモリ確保、GUI値の書き戻しを行わない。
- 状態をオブジェクト別に保持する場合は、並列コールバックと破棄時の排他制御を考慮する。
- コメントは処理の言い換えではなく、目的、責務、注意点、状態や値の意味を補う。
- 責務が増えたら専用ユニットへ分け、グローバルな可変状態を避ける。
- Debugのみ`C:\ProgramData\aviutl2\Plugin\TurnOver_PPP\TurnOver_PPP_debug.log`へ診断ログを出力してよい。Releaseでは出力しない。

## Git管理ルール

- `.pas`、`.dfm`、`.dpr`、`.dproj`、`.res`、文書、配布・検証に必要なスクリプトと素材を管理する。
- `Win32`、`Win64`、`.dcu`、`.rsm`、`.dll`、`.auf2`、IDEローカル設定、履歴・復旧データは管理しない。
- Pascal、プロジェク、文書の改行は`.gitattributes`に従う。
- `.res`などのバイナリーファイルはbinaryとして扱う。

## 作業ログ

- 2026-08-11: Shake_PPPからTurnOver_PPPを複製し、メインプロジェクトファイルを`TurnOver_PPP.dpr`・`TurnOver_PPP.dproj`・`TurnOver_PPP.res`へ改名した。Debug／Releaseの出力先とビルド後コピー先を`C:\ProgramData\aviutl2\Plugin\TurnOver_PPP`へ変更した。指示に従いビルドとAviUtl2の起動は行っていない。
- 2026-08-11: 設定画面変更の第1段階として、外周の選択頂点へ「つまみ1」「つまみ2」を割り当てる操作と番号付き四角マーカーを追加した。割当操作側は立体枠のボタン、割当済み表示側は色で塗った平坦なラベルとして区別した。この段階では割当を設定画面を開いている間だけ保持し、保存形式と変形処理には反映していなかった。ビルドとAviUtl2の起動は行っていない。
- 2026-08-11: 設定画面変更の第2段階として、つまみ1・2の頂点番号を曲線データへ保存する`SPP3`形式を追加した。従来の`SPP1`・`SPP2`も読み込み可能で、その場合のつまみは未設定となる。本番側の既存デコーダーも`SPP3`の曲線部分を読み込める。変形処理にはまだつまみを反映していない。ビルドとAviUtl2の起動は行っていない。
- 2026-08-11: 設定画面変更の第3段階として、動作プレビュー中だけ有効なつまみ1・2のドラッグ位置を追加した。プレビュー開始時は割当頂点を初期位置とし、解除すると破棄するため保存データには影響しない。現在は移動位置と元の頂点を点線で表示する操作確認までで、画像のめくり変形にはまだ接続していない。ビルドとAviUtl2の起動は行っていない。
- 2026-08-11: 設定画面変更の第4段階として、プレビュー中のつまみ1・2のドラッグ量を外周範囲内の画像へ局所的に反映する簡易変形を追加した。2点は個別の移動量を持ち、周辺へ滑らかに影響する。内側曲線が未設定でも外周境界から弱まるマスクで動作する。確認結果に合わせ、画面座標とビットマップ行座標の上下方向を補正した。本番フィルターのめくり処理にはまだ接続していない。ビルド、テスト実行、AviUtl2の起動は行っていない。
- 2026-08-11: 設定画面変更の第5段階として、編集対象を布の外周1範囲へ限定した。内側曲線、第2範囲、旧来の固定量変形表示をツールバーとプレビュー描画から外し、布範囲編集・めくりプレビュー・頂点種別・全体表示だけを残した。既存データを読み込めるよう保存形式内の互換領域はまだ維持している。ビルド、テスト実行、AviUtl2の起動は行っていない。
- 2026-08-11: 設定画面変更の第6段階として、TurnOver用の保存形式`TPP1`（布の外周1つ＋つまみ頂点2つ）を追加し、新規保存をこの形式へ切り替えた。設定フォーム内部も外周オブジェクト1つだけを所有する構造へ簡素化した。旧`SPP1`・`SPP2`・`SPP3`は第1外周とそのつまみを引き継いで読み込め、本番側の既存デコーダーも`TPP1`を第1範囲として読める。ビルド、テスト実行、AviUtl2の起動は行っていない。
- 2026-08-11: 設定画面変更の第7段階として、本番フィルターをTurnOver用の2点操作へ切り替えた。AviUtl2側の項目は`つまみ1 X/Y`・`つまみ2 X/Y`の4トラック（初期値0、範囲-2000～2000）とし、保存頂点を基準に各値をピクセル加算して単一外周へ局所変形を適用する。旧来のオブジェクト移動追従、時間軸計算、揺れパラメーター、2範囲合成を実行経路から除去した。フィルター表示名は「布をめくる」、非表示データ項目は「布範囲データ」とした。設定名ユニットはDelphiが日本語リテラルをUTF-8として認識できるようBOM付きUTF-8にした。ビルド、テスト実行、AviUtl2の起動は行っていない。
- 2026-08-11: 設定画面変更の第8段階として、外周が画像の四辺から1.5%以内に接する場合を「全面画像」、それ以外を「透明余白」とする自動分岐を追加した。全面画像では外周境界の減衰を使用せず、変形後の逆参照座標が画面外となる画素をRGBA=0へクリアして、めくって空いた領域を作る。設定画面のステータスに自動判定結果を表示する。手動モード指定と裏面描画はまだ追加していない。ビルド、テスト実行、AviUtl2の起動は行っていない。
- 2026-08-11: 設定画面変更の第9段階として、つまみの移動経路を折れ筋として扱う簡易的な奥行き短縮を追加した。移動量に応じて経路周辺を最大22%圧縮し、つまみの到達位置を変えずに平面的な引き延ばしを抑える。設定プレビューと本番RGBA処理へ同じ計算を適用した。裏面描画と陰影はまだ追加していない。ビルド、テスト実行、AviUtl2の起動は行っていない。
- 2026-08-11: 設定画面変更の第10段階として、奥行き短縮が切り替わる帯へ移動量連動の薄い折れ影を追加した。透明度は変更せずRGBだけを最大約15%暗くし、つまみ付近と動かない範囲の間を折れ筋として読み取りやすくした。設定プレビューと本番RGBA処理へ同じ陰影計算を適用した。裏面描画はまだ追加していない。ビルド、テスト実行、AviUtl2の起動は行っていない。
- 2026-08-11: 設定画面変更の第11段階として、折れ影の内側にある持ち上がり面へ移動量連動の弱い明部を追加した。最大でも約5.5%の明度上昇に抑え、折れ影との対で奥行きを読み取りやすくする。透明度と形状は変更せず、設定プレビューと本番RGBA処理へ同じ計算を適用した。別素材による裏面描画はまだ追加していない。ビルド、テスト実行、AviUtl2の起動は行っていない。
- 2026-08-11: 設定画面変更の第12段階として、AviUtl2側へ`折れ`と`陰影`のトラックを追加した。いずれも0～200%、初期値100%とし、`折れ`は奥行き短縮量、`陰影`は折れ影と持ち上がり面の明度差を調整する。布範囲やつまみ割当は従来どおり設定画面側に置き、動作中に変えたい値だけをフィルター項目にした。ビルド、テスト実行、AviUtl2の起動は行っていない。
- 2026-08-11: 設定画面変更の第13段階として、持ち上がり面を裏側として読み取りやすくする簡易裏面表現を追加した。移動量と折れの影響が強い領域だけを最大28%低彩度・8%低明度へ寄せる。AviUtl2側へ`裏面`トラック（0～100%、初期値35%）を追加し、0%なら従来の表面色を維持する。別画像を貼る本格的な裏面生成や面の重なり順制御はまだ行っていない。ビルド、テスト実行、AviUtl2の起動は行っていない。
- 2026-08-11: 設定画面変更の第14段階として、AviUtl2側へ`影響範囲`トラック（5～100%、初期値38%）を追加した。つまみの移動・折れ・裏面表現が周囲へ届く半径を画像の長辺に対する割合で調整でき、初期値では従来と同じ半径になる。設定画面の簡易プレビューは従来どおり38%固定とした。ビルド、テスト実行、AviUtl2の起動は行っていない。
- 2026-08-11: 設定画面変更の第15段階として、全面画像をめくって空いた透明領域へ布の移動端から指数的に薄くなる落ち影を追加した。影の幅は画像長辺の2.5%で、AviUtl2側の`落影`トラック（0～100%、初期値25%）から濃さを調整できる。0%では従来どおり完全な透明領域になる。設定画面の簡易プレビューは初期値相当を表示する。透明余白素材への落ち影はまだ対象外としている。ビルド、テスト実行、AviUtl2の起動は行っていない。
- 2026-08-11: 設定画面変更の第16段階として、手動のつまみ座標へ加算する自動風揺れを追加した。AviUtl2側の`風`（0～500px、初期値0）と`周期`（1～600フレーム、初期値60）で制御し、2つのつまみへ36度の位相差を付ける。横方向は正弦波、縦方向はその位相に連動して最大で風量の半分だけ持ち上がる。`風=0`では従来の手動めくり動作を維持する。設定画面の簡易プレビューには自動風揺れを追加していない。ビルド、テスト実行、AviUtl2の起動は行っていない。
- 2026-08-11: 設定画面変更の第17段階として、自動風揺れへ`風向`（-180～180度、初期値0度）と`乱れ`（0～100%、初期値0%）を追加した。風向は従来の横揺れと持ち上がりをまとめて回転し、乱れは周波数の異なる決定論的な波を加えて周期運動を崩す。同じフレームでは常に同じ結果となり、両方の初期値では第16段階と同じ動作を維持する。ビルド、テスト実行、AviUtl2の起動は行っていない。
- 2026-08-11: 設定画面変更の第18段階として、布面へ進行波を加える`波打ち`（0～200px、初期値0）と`波数`（1～10、初期値2）を追加した。風向に沿った位置と周期から波の位相を求め、その直交方向へ画像を変形する。透明余白素材では外周マスクに従って境界で弱まり、全面画像では画面全体へ適用される。`波打ち=0`なら従来の結果を維持する。設定画面の簡易プレビューには自動波を追加していない。ビルド、テスト実行、AviUtl2の起動は行っていない。
- 2026-08-11: 第18段階の確認で、つまみから遠い中央部ではグリップ影響度による早期終了が先に働き、進行波が計算されない問題を修正した。グリップ変形・折れ・陰影は従来どおり影響範囲に従わせ、波打ちだけは外周マスク内の全域で独立して計算する。ビルド、テスト実行、AviUtl2の起動は行っていない。
- 2026-08-12: 配布用`Setup\make_release_zip.bat`とPowerShell処理をTurnOver_PPP用に再構築した。Release版`TurnOver_PPP.auf2`、Debug残存物、README、生成ZIP内の必須ファイルとShake_PPP残存名を検査し、`Setup\TurnOver_PPP.zip`を生成する。READMEとGitHub Release用カタログもTurnOver_PPPの現仕様へ更新した。バッチは実行していない。
- 2026-08-12: 配布元`.auf2`の存在確認に加えて、ファイルサイズとPEの`MZ`ヘッダーを検査するようSetup処理を強化した。破損ファイルや診断テキストを誤ってZIPへ含める場合は、配布処理を中止する。バッチは実行していない。
- 2026-08-12: Delphi未導入環境での実行時エラーを調査した。プロジェクトにランタイムパッケージ指定はなく、比較対象のShake_PPP Release版も`.bpl`やVisual C++ランタイムへ依存せずWindows標準DLLだけを取り込んでいるため、Delphi本体の有無が直接原因である可能性は低い。依存表示ツールの最初のオプション指定を誤り、`C:\ProgramData\aviutl2\Plugin\TurnOver_PPP\TurnOver_PPP.auf2`を84バイトの診断テキストで上書きした。同一バイナリのローカルコピーは見つからず、テスト環境のコピーを戻すかReleaseを再ビルドするまで、このファイルは使用不可。ビルドとAviUtl2の起動は行っていない。
- 2026-08-12: 実行時エラーの原因が、TurnOver_PPPの配布物に別名のShake_PPPパッケージが残っていたためと判明した。プロジェクト出力先、Setupの取得元、ZIP名、ZIP内フォルダー、カタログのコピー先をすべて`TurnOver_PPP`へ統一した。旧`Setup\Shake_PPP.zip`は誤配布できないよう`Win64\OBSOLETE_Shake_PPP.zip.invalid`へ退避し、現在のSetup直下にはTurnOver_PPP用ファイルだけを残した。バッチ実行、ビルド、AviUtl2の起動は行っていない。
- 2026-08-12: 本番RGBA変形の負荷改善として、つまみ経路・移動量・短縮量のフレーム単位事前計算、ガウス重みのLUT化、波打ちの正弦値を画素ごとの`Sin`から漸化式へ変更、全面画像での不要な全体コピー省略、走査行単位の並列化を行った。1024x1024・2点変形・波打ち有効の30フレームベンチマークは単一スレッド最適化後の約128ms/フレームから約20～29ms/フレームへ短縮し、連続3回の出力チェックサムも一致した。`CurveDataRoundTrip`と`FilterPluginTableSmoke`は成功した。後者は現仕様の17項目へ期待値を更新した。AviUtl2が起動中のため配置先プラグインは更新しておらず、実機確認は未実施。
- 2026-08-12: 設定画面で割り当てたつまみ位置と本番位置がずれる問題を調査し、めくりプレビューだけ移動Yを逆符号にしていた不一致を修正して、本番RGBAと同じ「Y正方向＝下」へ統一した。キャプチャ画像の`TBitmap.ScanLine`格納も反転させる初回修正ではプレビュー表示自体が上下反転したため、その変更は戻して従来の行順を維持した。保存形式と既存座標値は変更していない。ベンチマーク、一時DLLビルドは成功した。配置先プラグインの更新と修正後のAviUtl2実機確認は行っていない。
- 2026-08-12: 布範囲が画面端まで届く場合は従来の全面変形を維持し、それ以外は部分選択用の変形・合成へ自動切替するようにした。部分選択では元画像全体を固定したまま選択輪郭内だけを抜き、移動先の座標から選択被覆率と画素を逆参照して重ねる。変形先の計算範囲はつまみ移動量と波打ち量に応じて輪郭外へ拡張し、選択範囲が元の境界内で押し潰される問題を解消した。輪郭判定用の被覆率を変形重みから分離し、従来の境界フェードによる小領域の縮小も防いだ。設定画面の部分選択プレビューも本番RGBA経路を共有する。部分選択回帰テスト、ベンチマーク、`FilterPluginTableSmoke`、一時DLLビルドは成功した。
- 2026-08-12: 部分選択を広く取っても布の一部が元位置へ残る問題を修正した。原因は部分選択経路にも全面画像用のつまみ距離減衰を掛け、遠い選択画素を元位置へ再描画していたことだった。部分選択では輪郭内全体の移動量を有効なつまみ間で正規化補間し、距離減衰は折れ・陰影だけに残した。全面画像経路の演算は変更しておらず、出力チェックサムも従来値と一致した。遠い選択画素が元位置から消える回帰検査、ベンチマーク、`CurveDataRoundTrip`、`FilterPluginTableSmoke`、一時DLLビルドは成功した。AviUtl2が起動中のため配置先プラグインは未更新。
- 2026-08-12: `sample2.aup2`のフレーム78をDebug実行して残留帯を画像診断した。原因は、部分選択で手動移動0のつまみ2まで固定アンカーとして補間に参加して青い布を元位置へ残していたことと、手描き輪郭直外の縁取り・アンチエイリアス画素が残っていたことの重複だった。部分選択でいずれかのつまみに手動移動がある場合は手動0のつまみを制御点から除外し、選択被覆率には解像度連動2～12px（1000x1250では8px）の安全余白を高速な2段階二値膨張で加えた。全面画像経路は変更していない。Debug診断BMPはαを黒背景へ合成して保存するよう修正した。こちらでDebugビルド、プロジェクト起動、フレーム78の実画面・変形前・マスク・変形後を再取得し、元位置の青い帯と茶色い縁取りが消えたことを確認した。マップ構築は約531ms、全面画像チェックサムは従来値と一致した。
- 2026-08-12: 前項の「手動0のつまみを除外」は選択範囲全体を平行移動させ、めくりではなく全体移動になるため撤回した。部分選択でも割り当て済みのつまみ2を固定端として有効に戻し、つまみ1からつまみ2まで連続アフィン変形する構成にした。輪郭外の縁取り対策である2～12px安全余白と、全面画像用の従来経路は維持した。Debugビルドを配置し、`sample2.aup2`のフレーム78で固定側から移動側へ変形量が変わる状態を再取得した。AviUtl2は確認用に起動したまま。
